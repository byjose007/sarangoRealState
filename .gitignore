node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
.env
.env*.local
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# Prisma-generated client — regenerated by `npx prisma generate` (postinstall)
src/generated/prisma

# local uploads volume (dev only — prod uses the docker volume)
/uploads

# Postgres dumps written by scripts/backup-db.sh — copy these off-server,
# never commit them (they contain client PII).
/backups
